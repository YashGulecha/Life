# Pulse - Life OS Environment Variables Template
# Copy this to backend/.env and replace values

# Secret key used to encrypt JWT session cookies. Keep this private!
# Generate using: openssl rand -hex 32
JWT_SECRET=your-random-32-byte-hex-string

# SQLite database file path inside the backend container
DATABASE_URL=sqlite:///app/data/life.db
